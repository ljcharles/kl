{% extends "KLCoreBundle::layout.html.twig" %}

{% block title %}Supprimer une gamme - {{ parent() }}{% endblock %}

{% block body %}

<div class="jumbotron" style="height: 350px;text-align: center;font-size: 5em;">
  <h2 style="margin-top: 75px;">Supprimer une gamme</h2>

  <p>
    Etes-vous certain de vouloir supprimer la gamme "{{ gamme.nom }}" ?
  </p>

  <form action="{{ path('kl_restauration_gamme_delete', {'id': gamme.id}) }}" method="post">
    <a href="{{ path('kl_restauration_gamme_view', {'id': gamme.id}) }}" class="btn btn-default">
      <i class="fa fa-undo"></i>
      Retour à la gamme
    </a>

    <input type="submit" value="Supprimer" class="btn btn-danger">
    {{ form_rest(form) }}
  </form>
</div>
{% endblock %}
